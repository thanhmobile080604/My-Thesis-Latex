@startuml activity_camera_optical_flow
' Render: tạo ảnh figures/activity_camera_optical_flow.png cho mục 2.3.3
skinparam defaultFontName "Times New Roman"
title Biểu đồ hoạt động: Xem Optical Flow trên Camera

|Người dùng|
start
:Chọn thuật toán KLT hoặc Farneback;
:Chọn chế độ hiển thị và độ nhạy;
|Hệ thống|
:Mở camera (CameraX, ImageAnalysis KEEP_ONLY_LATEST);
while (Còn frame camera?) is (có)
  :Chuyển frame sang Mat và chạy thuật toán;
  :Phủ vector/heatmap/motion trail lên khung hình;
  if (Người dùng khoanh ROI?) then (có)
    :Object tracker CSRT + template matching bám ROI;
    :Chạy Optical Flow trong vùng con;
  else (không)
  endif
  if (Người dùng ghi/chụp?) then (có)
    :Ghi video hoặc lưu ảnh kết quả;
  else (không)
  endif
endwhile (không)
stop
@enduml
