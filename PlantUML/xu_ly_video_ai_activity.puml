@startuml activity_xu_ly_video_ai
' Render: tạo ảnh figures/activity_xu_ly_video_ai.png cho mục 2.3.5
skinparam defaultFontName "Times New Roman"
title Biểu đồ hoạt động: Xử lý video Optical Flow bằng mô hình mạng lưới học sâu

|Người dùng|
start
:Chọn video đầu vào;
:Chọn backend (My/Outer) và chế độ xử lý;
|Hệ thống|
if (Video lớn?) then (có)
  :Tạo upload session;
  :Gửi từng chunk (SHA-256), ráp lại ở máy chủ;
else (không)
  :Gửi multipart request;
endif
:Máy chủ tạo job;
if (Backend nạp được mô hình?) then (có)
  :Chạy RAFT (ONNX Runtime), ghi MP4 H.264 (ffmpeg);
  :Cutie lan truyền mask nếu có ROI;
  :Foreground worker (WorkManager) theo dõi tiến độ;
  :Tải kết quả, mở video, dọn tài nguyên máy chủ;
else (không)
  :Job thất bại, hiển thị thông báo lỗi;
endif
stop
@enduml
